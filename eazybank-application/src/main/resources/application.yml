spring:
  application:
    name: eazybank-application
#  security:
#    user:
#      name: admin
#      password: admin


logging:
  pattern:
    console: "%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) -%msg%n"
  level:
    org.springframework.security: TRACE
